export interface Input {
  label: string
  modelValue: string
  name: string
  readonly?: boolean
  type?: string
  disabled?: boolean
  placeholder?: string
}

export interface Password {
  label: string
  modelValue: string
  name: string
}
